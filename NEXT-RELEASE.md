# 다음 릴리스 때 할 일 — 학급 메모 빈 화면 안내 문구

작성 2026-09-04 · 집 PC에서 정리, **노트북에서 마무리**

---

## 왜 고치나

GAS **v4.16**에서 설정 시트의 「긴급 공지」·「학급 메모」 칸을 없애고 **「학급공지」 시트**로 옮겼다.
공지는 이제 교사용 화면 오른쪽 위 **「📢 우리 반 공지」** 버튼에서 반별로 쓴다.

그런데 클라이언트(APK·EXE)는 메모가 비었을 때 이렇게 안내한다:

> 설정 시트 "학급 메모"에 문구를 입력하면 여기에 표시됩니다.

**이제 설정 시트에 그 칸이 없다.** 선생님이 없는 칸을 찾아 헤매게 된다.

> **동작은 멀쩡하다.** `?api=board` 응답 키(`notice`/`classMemo`/`noticeStep`/`memoStep`)를 그대로
> 뒀기 때문에 지금 깔린 APK·EXE도 새 공지를 정상으로 그린다. 이 문구 한 줄만 옛말이라,
> 급하지 않고 **다음 릴리스에 묻어 나가면 된다**(메모를 쓰고 나면 안 보이는 문구다).

---

## 상태

| 대상 | 저장소 | 상태 |
|---|---|---|
| APK 기본판 | `kimju1416/youcall-board` | ✅ **고쳐서 push 완료** (`cfd6098`) — 릴리스만 남음 |
| APK 호환판 | `kimju1416/youcall-board-compat` | ✅ **고쳐서 push 완료** (`b76d79d`) — 릴리스만 남음 |
| EXE (유콜 데스크) | `youcall-desktop` | ❌ **아직 안 고침** — 집 PC에 소스가 없다 |

---

## EXE 고치기 (노트북에서)

### 1. 고칠 자리 찾기

```bash
cd <youcall-desktop 폴더>
grep -rn '설정 시트' --include=*.js --include=*.html . | grep -v node_modules
```

`renderClassMemo` 안의 이 줄이 나올 것이다 (APK와 같은 계보라 문구가 같다):

```js
else { el.textContent = '설정 시트 "학급 메모"에 문구를 입력하면 여기에 표시됩니다.'; el.classList.add('empty'); }
```

> 못 찾으면 `grep -rn "학급 메모" --include=*.js --include=*.html . | grep -v node_modules` 로 넓혀 본다.
> 파일은 렌더러 쪽(`renderer/…` 또는 `src/…`)에 있다. `main.js`가 아니다.

### 2. 이렇게 바꾼다

```js
else { el.textContent = '교사용 화면의 「우리 반 공지」에서 메모를 적으면 여기에 표시됩니다.'; el.classList.add('empty'); }
```

바뀌는 건 **작은따옴표 안 문구 하나뿐**이다. 앞뒤 `el.classList.add('empty')`는 그대로 둔다.

⚠ **파이썬·sed로 고치지 말 것.** 한글·「」 때문에 파일이 깨진다. 편집기에서 직접 고치거나
Claude의 Edit 도구를 쓴다. ([[python-emoji-truncates-file]]에서 이 파일 65KB를 날린 적 있다)

### 3. 확인

```bash
node --check <고친 파일>          # 문법
grep -rn '설정 시트 "학급 메모"' . | grep -v node_modules   # 0건이어야 함
```

그리고 **실제로 띄워서** 메모가 빈 상태를 본다. 방법 두 가지:

- **간단**: 시트의 「학급공지」에서 그 반의 「학급 메모」 칸을 비우고 EXE를 3분 두거나 재시작
- **확실**: 이 저장소의 `tools/mock-gas-server.js`를 쓴다 (git pull로 같이 받아진다).

  ```bash
  node tools/mock-gas-server.js 8792
  ```

  EXE 설정의 웹앱 주소를 `http://localhost:8792/exec` 로 바꾸고, 브라우저나 curl로
  `http://localhost:8792/__set?case=empty` 를 부른 뒤 EXE를 다시 켠다.
  `case=merged|empty|long|single` 네 가지로 전환된다 —

  | case | 무엇을 보나 |
  |---|---|
  | `merged` | 전교 공지와 반별 공지가 ` · `로 합쳐진 상태 (4단계) |
  | `empty` | 공지·메모를 다 지운 상태 → **이번에 고친 문구가 여기서 보인다** |
  | `long` | 아주 긴 공지(5단계)가 잘리지 않고 자동 축소되는지 |
  | `single` | 반별 공지 하나만 (평범한 경우) |

  집 PC에서 APK로 이 네 가지를 다 확인했다(합쳐짐·배율 1.55, 긴 공지 1.75로 축소·안 잘림).
  EXE도 같은 결과가 나와야 한다.

### 4. 빌드·릴리스

```bash
npm run build:win
```

> `npm run dist` 아님 — 유콜 데스크는 `build:win`이다(쌤콜과 다르다).

릴리스 방법은 노트북의 기존 방식대로. 판 번호는 **v1.1.8**이 맞다(현재 v1.1.7).

---

## APK 릴리스 (노트북에서)

소스는 이미 고쳐져 있으니 **받아서 굽기만** 하면 된다.

```bash
cd <youcall-board 폴더>
git pull
bash build-release.sh 1.1.8 --release
```

스크립트가 판 번호 올리기·cap sync·빌드·인증서 확인·새 코드 반영 검사·릴리스·내려받기 대조까지 한다.
**키스토어(`youcall-release.keystore` + `keystore.properties`)가 없으면 빌드를 시작조차 하지 않는다** —
잘못된 키로 구우면 전국 사용자가 재설치해야 하므로 일부러 그렇게 막아뒀다.

호환판도 같이 낼 거면:

```bash
cd <youcall-board-compat 폴더>
git pull
# 호환판 키스토어는 집 PC에만 있다(youcall-compat.keystore) — 노트북에서 못 굽는다.
```

⚠ **호환판 키스토어는 집 PC에만 있다.** 노트북에서는 기본판만 낼 수 있다.
호환판은 집 PC에서 따로 굽거나, 급하지 않으니 다음에 함께 내도 된다.

---

## 릴리스 후 확인

1. 새 APK를 전자칠판에 덮어쓰기 설치 (서명 SHA-256 `de464c69…` 가 같아야 덮어쓰기가 된다 —
   `build-release.sh`가 자동으로 확인하고 다르면 멈춘다)
2. 「학급공지」 시트에서 그 반 메모를 비우고 3분 기다린다
3. 화면에 **「교사용 화면의 「우리 반 공지」에서 메모를 적으면 여기에 표시됩니다」** 가 뜨면 끝

---

## 참고 — 이번에 바뀐 것 요약

- 공지는 **「학급공지」 시트**에 반별로 쌓인다 (`학급 | 긴급 공지 | 공지 크기 | 학급 메모 | 메모 크기 | 작성자 | 수정시각`)
- 학급이 `전체`인 행은 전교 공통. 그 반 행과 **` · `로 합쳐져** 한 줄로 내려온다
- 글자 크기는 합쳐진 것 중 **가장 큰 단계**가 이긴다 (넘치면 클라이언트가 자동 축소)
- 교사목록에 **「담임학급」 C열**이 생겼다 (`1-2` 형식) — 적어두면 공지 쓸 때 자기 반이 자동 선택
- `?api=board` **응답 키는 하나도 안 바뀌었다** — 그래서 클라이언트 재빌드 없이도 동작한다
