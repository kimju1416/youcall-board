## 설정이 "반 입력에서 안 넘어가던" 문제를 고쳤습니다

전자칠판에서 **주소·학년·반을 다 넣었는데 [저장하고 시작하기]를 누를 수 없거나, 눌러도 아무 반응이 없던** 문제입니다.

### 무엇이 문제였나

| 증상 | 원인 |
|---|---|
| 반까지 넣었는데 **[저장] 버튼을 누를 수 없다** | 소프트 키보드가 버튼을 가림. 반이 마지막 칸이라 입력을 마치면 항상 이 상태가 됩니다 |
| **칸들이 서로 겹쳐 보인다** | 키보드로 화면이 좁아지면 「동작 방식」 라벨이 학년 칸 위로 올라탐 |
| 저장을 눌러도 **아무 반응이 없다** | 주소를 `https://` 없이 넣으면 내부 오류가 나는데, 그게 화면에 안 뜨고 그대로 멈춤 |
| 다 채웠는데 **"모두 입력하세요"** 만 뜬다 | 학년·반의 예시 글자(`3`, `2`)가 실제 입력값처럼 보여서 |

### 어떻게 고쳤나

- **반 칸을 누르면 [저장] 버튼이 따라 올라옵니다.** 그래도 안 보이면 **키보드의 → (엔터)를 눌러도 저장**됩니다
- 칸이 눌려 겹치지 않습니다
- 주소에 `https://`를 빼먹어도 **자동으로 붙여줍니다** (앞뒤 공백, `?role=teacher`도 알아서 정리합니다)
- 학년·반은 **숫자 키패드**가 뜨고, 예시 글자는 `예: 3`으로 바뀌었습니다
- 무슨 문제가 생기든 이제 **화면에 글자로 남습니다** (예전엔 조용히 멈췄습니다)

### 오래된 전자칠판도 이제 됩니다

칠판의 웹 표시 엔진(WebView)이 **공장 출하 상태로 낡은 기기**에서는 앱이 쓰는 최신 기능 몇 가지가 없어 저장 단계에서 막혔습니다. 안드로이드 버전이 8이어도 WebView를 한 번도 갱신하지 않았으면 2017년 버전에 머물러 있습니다.

- 요청 시간을 재는 방식, 서버에 요청을 보내는 방식을 **옛 엔진에서도 되는 방법**으로 바꿨습니다
- 시간표 글자 크기와 설정 화면 여백에 **대체 값**을 넣어 화면이 무너지지 않게 했습니다
- 서버가 예상과 다른 형태로 답해도 **화면 전체가 멈추지 않습니다**

### 지금 잘 쓰고 계신 칠판은 그대로입니다

수정 전후 화면을 **1920 · 1366 · 1280 세 해상도 × 대기화면 · 설정화면**으로 나란히 그려 대조했습니다. **요소 위치가 전부 일치**하고, 눈에 보이는 차이는 위에 적은 예시 글자뿐입니다. 바뀐 동작은 모두 "옛 엔진일 때만" 갈라지도록 되어 있습니다.

---

## 설치

기존 앱 위에 **그대로 덮어쓰기 설치**하시면 됩니다 (설정은 유지됩니다).

> 💡 저장 후 **"연결 확인 중..."** 이 보이면 최대 1분 기다려 주세요. 구글 서버가 깨어나는 시간입니다.
> 💡 **상주형(트레이)** 으로 쓰실 거면 첫 실행 때 뜨는 **「다른 앱 위에 표시」 권한을 꼭 켜주세요.** 이 권한이 없으면 다른 화면을 보고 있을 때 호출이 위로 올라오지 못합니다.

---

문의 · 버그 제보 : Instagram [@kimju.zip](https://instagram.com/kimju.zip)
